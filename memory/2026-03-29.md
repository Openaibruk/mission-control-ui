# Daily Memory Log: 2026-03-29

## Mission Control Dashboard Fixes
- Addressed an issue where Vercel deployments were constantly reverting to an old v1 dashboard backup due to Next.js build failures (missing `SUPABASE_URL` in the static page pre-render step) and misconfigured directory paths (`apps/mission-control-ui` vs root).
- Disabled the `Daily B2B Snapshot` cron job (`7239e65e-907a-4021-8927-d67bf2699c7a`) that was automatically pushing broken code and triggering Vercel rebuilds.
- Force-pushed the exact desired commit (`5cfdcc8`, "Mission Ctrl v2.1") directly to `main` and hard-aliased the Vercel live URL (`mission-control-ui-sand.vercel.app`) to a stable deployment container (`l1odgyqq8` / `ovnaepjz5`) to permanently lock the preferred dashboard version in place.

## New Major Project: Mission Control v3 Upgrade (Data-Driven Scalable OS)
- **Objective:** Transform the static Mission Control dashboard into a real-time system connected to Supabase, ClickHouse, and MCP server data.
- **Structure:**
  - **Phase 1 (Completed via subagent `mc-v3-phase1`):** Fixed light mode readability (Ethiopian Calendar, News cards), replaced shallow Project Card popups with a deep `ProjectOverview` page, and upgraded the Activity Stream to capture real agent events with multi-filtering.
  - **Phase 2:** Agents (dynamic listing, soul.md editor, tools/model config) & Live metrics (token costs, active tasks, streaming logs).
  - **Phase 3:** Enhanced sortable Projects/Skills tables and Skill Detail View.
  - **Phase 4:** Connect real Analytics (B2C/B2B data) and audit Settings fields.
  - **Phase 5:** Add a central Files page and a visual Workspace Management folder explorer.
- **Artifact:** Blueprint saved to `/projects/active/mc-v3-upgrade.md`.