
## Self-Audit Heartbeat — 12:03 PM
- Cron `self-audit-heartbeat` fired but `scripts/self-audit.js` doesn't exist
- Ran manual audit instead: all systems healthy (disk 75%, mem ok, gateway running, no errors)
- Action needed: create the audit script or update the cron job

## 2026-03-30 12:35 PM
- Fixed an issue in Mission Control where the Agent Profile editor could not read `SOUL.md` on Vercel deployments. 
- Resolved by configuring the Vercel build to copy/bundle root workspace files (like `SOUL.md`) into the `public/` folder so the Next.js API route can read them from `process.cwd()` in the serverless environment.

## 2026-03-30 23:16 PM
- Fixed cron jobs by enforcing explicit provider/model path ('openrouter/stepfun/step-3.5-flash:free') across 13 jobs, preventing 'Unknown model' failovers.
- Disabled 'ipmr-feedback-review' job to remove overlap with 'Feedback Autopilot'.
- Disabled 'self-audit-heartbeat' since its target script doesn't exist yet.
- Slowed 'task-worker-heartbeat' from every 5 minutes down to every 15 minutes to save tokens.
