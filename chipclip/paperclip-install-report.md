# Paperclip Installation Report

**Date:** 2026-03-17  
**Agent:** @Henok (Dev + DevOps)  
**Status:** ✅ Successfully installed and running

---

## System Requirements

| Requirement | Status |
|-------------|--------|
| Node.js | ✅ v22.22.1 (required 20+) |
| pnpm | ✅ 10.32.1 (installed globally) |
| Git | ✅ 2.43.0 |
| Disk Space | ⚠️ Critical (6.8GB total, ~403MB free after install) |

## Installation Steps

1. **pnpm installed** via `npm install -g pnpm`
2. **Disk cleanup** required — freed ~550MB by:
   - `sudo apt-get clean` (removed 540MB apt cache)
   - Cleared npm cache and temp files
3. **Cloned repo** from `https://github.com/paperclipai/paperclip.git` (shallow clone, `--depth 1`)
4. **Installed dependencies** via `pnpm install --no-frozen-lockfile` (20 workspace projects, ~1067 packages)
5. **Database migrated** automatically — 35 migrations applied via embedded PGlite
6. **Dev server started** on port 3100

## Running Services

| Service | URL |
|---------|-----|
| API | `http://127.0.0.1:3100/api` |
| Health Check | `http://127.0.0.1:3100/api/health` → `{"status":"ok"}` |
| UI | `http://127.0.0.1:3100` (HTTP 200) |
| Database | Embedded PGlite on port 54329 (`/home/ubuntu/.paperclip/instances/default/db`) |

## Configuration

- **Deployment mode:** `local_trusted` (private)
- **Auth:** ready
- **SERVE_UI:** true
- **Database:** embedded-postgres (no external Postgres needed)
- **Agent JWT:** missing (requires `pnpm paperclipai onboard`)
- **Heartbeat:** enabled (30s)
- **DB Backup:** enabled (every 60m, keep 30 days)

## Issues & Warnings

1. **Disk space critical** — Server has only 6.8GB with 95% usage. The install consumed ~761MB for the repo + node_modules. Future operations (building, additional packages) may require cleanup.
2. **Plugin SDK bin warnings** — Several `paperclip-plugin-dev-server` bin links failed because the plugin SDK hasn't been built yet. Not blocking for basic operation.
3. **Peer dependency warnings** — `zod@^4.0.0` and `drizzle-orm@>=0.41.0` are expected but older versions are installed. Non-blocking.
4. **pnpx `paperclipai onboard` failed** initially due to disk space; used git clone approach instead. Agent JWT still needs to be configured.

## File Locations

- **Install directory:** `/home/ubuntu/.openclaw/workspace/paperclip-server/`
- **Dev log:** `/home/ubuntu/.openclaw/workspace/chipclip/paperclip-dev.log`
- **Database:** `/home/ubuntu/.paperclip/instances/default/db`
- **Config:** `/home/ubuntu/.paperclip/instances/default/config.json`
- **Backups:** `/home/ubuntu/.paperclip/instances/default/data/backups`

## Post-Install TODO

- [ ] Run `pnpm paperclipai onboard` to configure Agent JWT
- [ ] Monitor disk space — recommend upgrading to larger disk or regular cleanup
- [ ] Consider adding `PORT=3100` and `SERVE_UI=true` to `.env` for persistence (already created)

---

*Report generated by @Henok*
